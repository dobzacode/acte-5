'use client';

import { Input, extendVariants } from '@nextui-org/react';

export const UiInput = extendVariants(Input, {
  variants: {},
  compoundVariants: [],
  defaultVariants: {
    size: 'md'
  }
});

export default UiInput;
